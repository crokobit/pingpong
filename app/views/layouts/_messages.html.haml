.messages
  .container
    - flash.each do |name, msg|
      - if msg.is_a?(String)
        %div{ class: "alert alert-#{name.to_s == 'notice' ? 'success' : 'danger'}" }
          %div{ id: "flash_#{name}" }= msg
